//! App-wide constants.

/// Pagination and virtual scroll settings.
pub struct PAGINATION;

impl PAGINATION {
    /// Row height in pixels for virtual scroll calculations.
    pub const ROW_HEIGHT: usize = 36;
}
