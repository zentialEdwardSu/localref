[package]
name = "ui-wasm"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[target.'cfg(target_arch = "wasm32")'.dependencies]
ui-app = { path = "../ui-app", default-features = false, features = ["hydrate"] }
wasm-bindgen = "0.2"
