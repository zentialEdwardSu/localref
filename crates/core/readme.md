# ref-bib

`ref-core` is the core companies coponent for `localref`it is a dameon with tray cion. It runs the `ref-csc`,`ref-rest`,`ref-scan`, for getting data from zotero connector, exposing api to frontend, and scanning local filesystem for manual updates.